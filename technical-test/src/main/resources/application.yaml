spring:
  application:
    name: technical-test

  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
    h2:
      console:
        enabled: true
        path: /h2-console

jwt:
  secret: una_clave_super_segura_de_mas_de_32_caracteres!!
  expiration: 3600000